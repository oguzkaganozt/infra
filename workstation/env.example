# Minimal secrets to store in Infisical. Bootstrap flags carry machine intent.

TS_AUTHKEY='<tailscale-auth-key>'
RCLONE_CONFIG_B64='<base64-encoded-rclone.conf>'
WORKSTATION_PASSWORD='<strong-nomachine-password>'

# Optional secret for GitHub HTTPS access.
# GITHUB_TOKEN='<github-token>'
